backend:
    build:
      context: ./Iot-backend
      dockerfile: Dockerfile
    container_name: iot-backend
    ports:
      - "3001:3001"
    environment:
      DB_TYPE: postgres
      DB_HOST: 148.230.98.12
      DB_PORT: 5432
      DB_USERNAME: postgres
      DB_PASSWORD: D4t4bas3N3TKR1da
      DB_DATABASE: iot_monitoring
      DB_SSL: false
      MQTT_BROKER: tcp://test.mosquitto.org:1883  # sama dengan ESP8266
      MQTT_USERNAME: ""
      MQTT_PASSWORD: ""
      MQTT_TOPIC_DATA: iot/power
      MQTT_TOPIC_CONTROL: iot/power/control
      MQTT_TOPIC_REBOOT: iot/power/reboot
      MQTT_BATCH_INTERVAL: 5000
      MQTT_BUFFER_MAX_SIZE: 1000
      PORT: 3001
      NODE_ENV: production
      CORS_ORIGIN: http://localhost:3000
      LOG_LEVEL: info
    restart: unless-stopped
    networks:
      - iot-network